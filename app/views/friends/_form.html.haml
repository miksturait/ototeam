= simple_form_for @friend, :html => { :class => "form-horizontal" } do |f|


 = f.input :name
 = f.input :numer_tel
 = f.input :email
 #= f.association :group, label_method: :email
 = f.button :submit